---
title: "创建自定义动画"
date: 2026-08-07
excerpt: "从零制作你自己的文字动画——创建 UTextAnimationBlueprint、编辑字形动画曲线、编排图层并发布为预设。"
---

# 创建自定义动画

本节引导你从零制作属于自己的文字动画：创建动画蓝图资产 → 添加字形动画 → 覆盖 BuildDefaultAnimation → 在 UMG 中播放。

> [!TIP]
> 如果你还没有用过内置预设，建议先完成《使用预设动画》熟悉控件与播放流程，再进入本节学习制作。

## 创建文本动画蓝图资产

适用场景：开始制作第一个自定义动画蓝图资产。

`UTextAnimationBlueprint` 是 Texturge 的核心创作资产。每个动画蓝图持有一组 `UGlyphAnimation` 曲线资产，设计师声明的蓝图变量即动画参数（预设库的 `Duration` / `CharDelay` 等参数就是这样实现的）。

1. 内容浏览器右键 → **Texturge** 分类 → **文本动画蓝图**（`UTextAnimationBlueprintFactory`）
2. 命名资产，如 `TAB_Wave`
3. 双击打开文本动画蓝图编辑器

<figure class="img-placeholder"><figcaption>images/create-text-animation-blueprint.png — 内容浏览器右键菜单截图：Texturge 子菜单展开，高亮"文本动画蓝图"项，资产命名为 TAB_Wave — Content Browser context menu screenshot: Texturge submenu expanded with "文本动画蓝图" (Text Animation Blueprint) highlighted, asset named TAB_Wave</figcaption></figure>

## 熟悉编辑器

适用场景：首次打开编辑器时了解各面板功能。

`FTextAnimationBlueprintEditor` 提供 **Designer / Graph 双模式**与 7 个标签页：

| 标签页 | 说明 |
|:---|:---|
| **设计器**（AnimationDesigner） | WYSIWYG 设计视口（`STextAnimationDesignerView`），缩放/平移/网格/字体度量 |
| **动画列表**（PreviewAnimationList） | 预览动画列表（`STextAnimationPreviewList`） |
| **时间轴**（Timeline） | Sequencer 集成（`UGlyphAnimation` 关键帧编辑） |
| **详情**（Details） | 蓝图详情面板 |
| **预览设置**（PreviewSettings） | 预览参数（文本/字体/视口尺寸） |
| **轨道列表**（TrackList） | 旧版轨道列表（兼容） |
| **预览视口**（PreviewViewport） | 预览文本动画控件 |

<figure class="img-placeholder"><figcaption>images/editor-layout.png — 文本动画蓝图编辑器全景截图：左侧动画列表面板，中央设计器视口显示预览文本 "Hello Texturge"，右侧 Sequencer 时间轴与详情面板，顶部工具栏含模式切换（Designer/Graph）与"编译"按钮 — Animation blueprint editor panoramic screenshot: animation list panel on the left, designer viewport showing "Hello Texturge" preview text in the center, Sequencer timeline and details panel on the right, mode switcher (Designer/Graph) and Compile button in the toolbar</figcaption></figure>

## 添加字形动画资产

适用场景：为动画蓝图添加关键帧曲线资产。

`UGlyphAnimation` 是 `UMovieSceneSequence` 子类，存储 21 通道关键帧曲线。

1. 在 **动画列表** 面板点击 **+** 创建新的**字形动画**资产（`UGlyphAnimation`，或选择已有资产）
2. 双击资产在 **时间轴** Sequencer 中打开
3. 点击 **添加子轨道**，选择轨道类型（不透明度 / 平移 / 缩放 / 染色等 10 种）
4. 在 FloatChannel 上添加关键帧并调整曲线（1 秒键轴为预设约定）

> [!TIP]
> 编辑器预览默认使用 `"Hello Texturge"` 预览文本（`UTexturgePreviewSceneConfig::PreviewText`），可在 **预览设置** 面板修改。

## 配置预览文本

适用场景：在编辑器中设置测试文本实时预览。

1. 切换 **Designer** 模式
2. 在 **预览设置** 面板输入测试文本（如 "Hello World"）
3. 选择预览源：**阶段预览**（StagePreview，阶段循环预览）或 **字形动画**（GlyphAnimation，当前动画预览）
4. 调整视口缩放与网格查看全部字形

## 编译动画蓝图

适用场景：完成配置后编译蓝图生成运行时烘焙数据。

1. 点击工具栏 **编译** 按钮
2. `FTextAnimationBlueprintCompilerContext` 执行 Kismet 编译，生成 `UTextAnimationBlueprintGeneratedClass`，`CompileCount` 递增
3. 编译后缓存失效，预览源自动切换（无 → 字形动画）触发重新烘焙
4. 在预览视口观察动画效果

> [!NOTE]
> 预览文本仅用于编辑器实时预览，不影响运行时 `UAnimatedTextBlock` 的实际显示文本。运行时文本由控件 `Text` 属性独立控制。

<figure class="img-placeholder"><figcaption>images/compile-blueprint.png — 编译结果截图：编辑器底部编译结果面板显示"编译成功"，设计器视口同步刷新预览动画 — Compile result screenshot: compiler results panel at the bottom showing successful compilation, designer viewport refreshing the preview animation</figcaption></figure>

## 在 UMG 中使用

适用场景：将编译好的动画蓝图绑定到 UMG 控件。

### 创建控件

1. 内容浏览器右键 > **用户界面 > Widget 蓝图**，命名 `WBP_HelloWorld`
2. 在**面板**中搜索 **动画文本块**，拖入画布
3. 调整控件尺寸与位置

<figure class="img-placeholder"><figcaption>images/umg-widget-setup.png — UMG 控件设计器截图：画布中 UAnimatedTextBlock 控件选中，右侧细节面板显示文本属性（"Hello World"）、"文本动画蓝图"属性（TAB_Wave）与"实例参数覆盖"属性 — UMG designer screenshot: UAnimatedTextBlock selected on canvas, Details panel showing Text property ("Hello World"), TextAnimationBlueprint property (TAB_Wave) and Parameter Overrides property</figcaption></figure>

### 绑定动画蓝图

1. 选中画布中的**动画文本块**控件（`UAnimatedTextBlock`），在**细节**面板设置 **文本动画蓝图** 为 `TAB_Wave`
2. 设置 **文本** 属性为运行时要显示的文本
3. 可选：勾选 **自动播放** 或 **循环播放**
4. 编译保存控件

### 通过参数覆盖微调

在**细节**面板展开 **实例参数覆盖**（`UAnimParameterOverrides`）的 6 组映射，按控件实例覆盖蓝图默认变量：

| 覆盖组 | 键 | 值类型 |
|:---|:---|:---|
| **浮点覆盖** | 变量名 | `float` |
| **整数覆盖** | 变量名 | `int32` |
| **布尔覆盖** | 变量名 | `bool` |
| **向量覆盖** | 变量名 | `FVector` |
| **颜色覆盖** | 变量名 | `FLinearColor` |
| **向量2D覆盖** | 变量名 | `FVector2D` |

每个变量行附带**重置**按钮（恢复蓝图默认值，CDO 永不修改）。

## 测试运行

适用场景：在 PIE 中验证游戏内实际播放效果。

1. 关卡中创建控件并添加到视口（关卡蓝图：`事件 BeginPlay` → `创建控件` → `添加到视口`）
2. 运行 PIE（`Alt+P`）
3. 文字按动画蓝图定义的动画播放

<figure class="img-placeholder"><figcaption>images/pie-result.png — PIE 运行截图：视口底部显示 WBP_HelloWorld 的逐字动画（如波浪弹入），文字处于播放中间状态，不同字形位置/透明度不同 — PIE screenshot: WBP_HelloWorld per-glyph animation (e.g. wave bounce-in) at the bottom of the viewport, glyphs in mid-animation with varying positions/opacity</figcaption></figure>

## 运行时控制

适用场景：在蓝图或 C++ 中动态控制播放与参数。

`UAnimatedTextBlock` 蓝图节点：

- `播放` / `暂停` / `恢复` / `停止` / `跳转到结尾` / `是否正在播放`
- `设置蓝图变量`（浮点/整数/布尔/向量/颜色/向量2D 6 重载）——运行时修改动画参数，触发冷却重烘焙（3 帧）
- 事件：`字形揭示（蓝图）`、`动画完成（蓝图）`

> [!NOTE]
> `VariableName` 须与动画蓝图**类默认值**（Class Defaults）中声明的变量名一致（`FName` 比较不区分大小写）。int32 / bool 类型使用 `设置蓝图变量 (Int)` / `设置蓝图变量 (Bool)` 重载。

至此，你已经完成了从创建资产到运行时播放的完整制作流程。

> [!TIP]
> 把你的自定义动画发布为预设：制作完成后，在**数据资产**中为它配置标签条目，即可像内置预设一样通过 `<anim id="...">` 标签在富文本中复用；进一步学习请前往《多图层动画》与《高级效果》。
