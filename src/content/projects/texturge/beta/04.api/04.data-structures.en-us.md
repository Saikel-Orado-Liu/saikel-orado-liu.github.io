---
title: Data Structures Reference
date: 2026-07-24
locale: en-us
---

# Data Structures Reference

Complete definitions of all USTRUCT and non-UObject data structures in Texturge Beta.

## USTRUCTs (Blueprint-Visible)

The following structs are marked `USTRUCT(BlueprintType)` and usable in Blueprint.

### FBakedStage

**File**: `Public/Animation/BakedAnimation.h`

Baked result for a single animation stage.

```cpp
USTRUCT(BlueprintType)
struct FBakedStage
{
    UPROPERTY(BlueprintReadOnly)
    EAnimationStage StageType;        // Intro / Default / Outro

    UPROPERTY(BlueprintReadOnly)
    int32 GlyphCount;                 // Number of glyphs

    UPROPERTY(BlueprintReadOnly)
    float TotalDuration;              // Total stage duration (seconds)

    UPROPERTY(BlueprintReadOnly)
    TArray<FBakedGlyph> Glyphs;       // Glyph array
};
```

Methods:
```cpp
bool IsValid() const;
int32 GetStartedGlyphCount(float StageTime) const;
```

### FBakedGlyph

**File**: `Public/Animation/BakedAnimation.h`

Per-glyph runtime baked data.

```cpp
USTRUCT(BlueprintType)
struct FBakedGlyph
{
    UPROPERTY(BlueprintReadOnly)
    int32 OriginalIndex;              // Position in source text

    UPROPERTY(BlueprintReadOnly)
    FString Character;                // The character itself

    UPROPERTY(BlueprintReadOnly)
    float StartTime;                  // Animation start time

    UPROPERTY(BlueprintReadOnly)
    float Duration;                   // Animation duration

    UPROPERTY(BlueprintReadOnly)
    bool bIsStatic;                   // Static glyph (no animation)

    UPROPERTY(BlueprintReadOnly)
    FGlyphAnimationState DefaultState; // Default render state

    UPROPERTY(BlueprintReadOnly)
    TArray<FPerLayerCurves> LayerCurves; // Multi-layer curve data
};
```

Methods:
```cpp
bool IsCurvesEmpty() const;
FVector2D GetCurvesTimeRange() const;
void CopyCurvesFrom(const FBakedGlyph& Other);
void ApplyCurvesScaleOffset(float Scale, float Offset);
void ApplyCurvesSeedJitter(float Jitter);
void OffsetCurvesBy(float Offset);
FBakedGlyph BuildCurveSet() const;
void AddLayer(const FPerLayerCurves& Layer);
bool HasStarted(float StageTime) const;
bool HasFinished(float StageTime) const;
void Evaluate(float StageTime, FGlyphAnimationState& OutState) const;
```

### FPerLayerCurves

**File**: `Public/Animation/BakedAnimation.h`

Single layer 21-channel independent curve storage. Each channel is stored as `TArray<FRichCurveKey>`.

```cpp
USTRUCT(BlueprintType)
struct FPerLayerCurves
{
    // Layer config
    UPROPERTY(BlueprintReadOnly) ETextAnimationBlendMode BlendMode;
    UPROPERTY(BlueprintReadOnly) EStagePlayDirection PlayDirection;
    UPROPERTY(BlueprintReadOnly) int32 NumberOfLoops;
    UPROPERTY(BlueprintReadOnly) float LayerDuration;

    // Float channels (3)
    UPROPERTY() TArray<FRichCurveKey> OpacityKeys;
    UPROPERTY() TArray<FRichCurveKey> LetterSpacingKeys;
    UPROPERTY() TArray<FRichCurveKey> RotationKeys;

    // Vector2D X/Y channels (10)
    UPROPERTY() TArray<FRichCurveKey> TranslationXKeys;
    UPROPERTY() TArray<FRichCurveKey> TranslationYKeys;
    UPROPERTY() TArray<FRichCurveKey> ScaleXKeys;
    UPROPERTY() TArray<FRichCurveKey> ScaleYKeys;
    UPROPERTY() TArray<FRichCurveKey> ShearXKeys;
    UPROPERTY() TArray<FRichCurveKey> ShearYKeys;
    UPROPERTY() TArray<FRichCurveKey> PivotXKeys;
    UPROPERTY() TArray<FRichCurveKey> PivotYKeys;
    UPROPERTY() TArray<FRichCurveKey> ShadowOffsetXKeys;
    UPROPERTY() TArray<FRichCurveKey> ShadowOffsetYKeys;

    // Color RGBA channels (8)
    UPROPERTY() TArray<FRichCurveKey> ColorRKeys;
    UPROPERTY() TArray<FRichCurveKey> ColorGKeys;
    UPROPERTY() TArray<FRichCurveKey> ColorBKeys;
    UPROPERTY() TArray<FRichCurveKey> ColorAKeys;
    UPROPERTY() TArray<FRichCurveKey> ShadowColorRKeys;
    UPROPERTY() TArray<FRichCurveKey> ShadowColorGKeys;
    UPROPERTY() TArray<FRichCurveKey> ShadowColorBKeys;
    UPROPERTY() TArray<FRichCurveKey> ShadowColorAKeys;
};
```

Methods:
```cpp
bool IsEmpty() const;
float GetEffectiveDuration() const;
void Evaluate(float LocalTime, FGlyphAnimationState& InOutState) const;
```

### FGlyphAnimationState

**File**: `Public/Animation/GlyphAnimationState.h`

Runtime render state for a single glyph.

```cpp
USTRUCT(BlueprintType)
struct FGlyphAnimationState
{
    UPROPERTY(BlueprintReadWrite)
    FVector2D PositionOffset = FVector2D::ZeroVector;

    UPROPERTY(BlueprintReadWrite)
    FVector2D Scale = FVector2D(1.0f, 1.0f);

    UPROPERTY(BlueprintReadWrite)
    float Rotation = 0.0f;

    UPROPERTY(BlueprintReadWrite)
    FVector2D Shear = FVector2D::ZeroVector;

    UPROPERTY(BlueprintReadWrite)
    FVector2D Pivot = FVector2D(0.5f, 0.5f);

    UPROPERTY(BlueprintReadWrite)
    FLinearColor Color = FLinearColor::White;

    UPROPERTY(BlueprintReadWrite)
    float Opacity = 1.0f;

    UPROPERTY(BlueprintReadWrite)
    FVector2D ShadowOffset = FVector2D::ZeroVector;

    UPROPERTY(BlueprintReadWrite)
    FLinearColor ShadowColor = FLinearColor::Transparent;

    UPROPERTY(BlueprintReadWrite)
    float LetterSpacing = 0.0f;

    static FGlyphAnimationState Identity();
};
```

### FGlyphScheduleInfo

**File**: `Public/Animation/GlyphScheduleInfo.h`

Per-glyph scheduling configuration.

See [Glyph Schedule Reference →](../reference/schedule-reference) for complete field descriptions.

### FSemanticAnchor

**File**: `Public/Localization/SemanticAnchor.h`

Cross-language text positioning anchor.

```cpp
USTRUCT(BlueprintType)
struct FSemanticAnchor
{
    UPROPERTY(BlueprintReadWrite)
    FName AnchorId;                   // Unique identifier

    UPROPERTY(BlueprintReadWrite)
    FString ContextualSnippet;        // Context snippet

    UPROPERTY(BlueprintReadWrite)
    int32 SourceOffset;               // Source text offset
};
```

### FAnimationEntry

**File**: `Public/Animation/AnimationEntry.h`

Tag-to-blueprint mapping. Parameter overrides now use 6 independent typed parameter maps, compatible with the `UAnimParameterOverrides` template system.

```cpp
USTRUCT(BlueprintType)
struct FAnimationEntry
{
    UPROPERTY(BlueprintReadWrite)
    FName TagName;                    // Tag name

    UPROPERTY(BlueprintReadWrite)
    UTextAnimationBlueprint* Type;    // Animation blueprint reference

    // Six independent typed parameter override maps
    UPROPERTY(BlueprintReadWrite)
    TMap<FName, float> ParameterOverrides;           // float overrides
    UPROPERTY(BlueprintReadWrite)
    TMap<FName, int32> IntParameterOverrides;        // int32 overrides
    UPROPERTY(BlueprintReadWrite)
    TMap<FName, bool> BoolParameterOverrides;        // bool overrides
    UPROPERTY(BlueprintReadWrite)
    TMap<FName, FVector> VectorParameterOverrides;   // FVector overrides
    UPROPERTY(BlueprintReadWrite)
    TMap<FName, FLinearColor> ColorParameterOverrides; // FLinearColor overrides
    UPROPERTY(BlueprintReadWrite)
    TMap<FName, FVector2D> Vector2DParameterOverrides;  // FVector2D overrides
};
```

### FStageTransitionConfig

**File**: `Public/Animation/StageTransitionConfig.h`

Stage transition configuration.

```cpp
USTRUCT(BlueprintType)
struct FStageTransitionConfig
{
    UPROPERTY(BlueprintReadWrite)
    FRichCurve IntroToDefaultCrossFade;

    UPROPERTY(BlueprintReadWrite)
    FRichCurve DefaultToOutroCrossFade;

    UPROPERTY(BlueprintReadWrite)
    EAlphaBlendOption EasingFunc;
};
```

## Non-USTRUCTs (C++ Only)

The following structs are not UPROPERTY-marked and are C++ only.

### FBakedAnimation

**File**: `Public/Animation/BakedAnimation.h`

Complete baked animation result. Uses `TUniquePtr` to own stage data.

```cpp
struct FBakedAnimation
{
    FString SourceText;
    int32 GlyphCount;
    uint64 TextFingerprint;
    uint64 BlueprintFingerprint;

    TUniquePtr<FBakedStage> IntroStage;
    TUniquePtr<FBakedStage> DefaultStage;
    TUniquePtr<FBakedStage> OutroStage;

    FBakedStage* GetStage(EAnimationStage Type);
    const FBakedStage* GetStage(EAnimationStage Type) const;
    bool HasStage(EAnimationStage Type) const;
    FBakedStage* GetFirstValidStage();
    EAnimationStage GetNextStageAfter(EAnimationStage Current) const;
    float GetTotalDuration() const;
};
```

### FGlyphCurveSet

**File**: `Public/Animation/GlyphCurveSet.h`

21 FRichCurve container, C++ internal only. No UPROPERTY.

```cpp
struct FGlyphCurveSet
{
    // Float curves
    FRichCurve Opacity;
    FRichCurve LetterSpacing;
    FRichCurve Rotation;

    // Vector2D component curves
    FRichCurve TranslationX, TranslationY;
    FRichCurve ScaleX, ScaleY;
    FRichCurve ShearX, ShearY;
    FRichCurve PivotX, PivotY;
    FRichCurve ShadowOffsetX, ShadowOffsetY;

    // Color component curves
    FRichCurve ColorR, ColorG, ColorB, ColorA;
    FRichCurve ShadowColorR, ShadowColorG, ShadowColorB, ShadowColorA;

    void Evaluate(float Time, FGlyphAnimationState& OutState) const;
    void OffsetBy(float Offset);
    void CopyFrom(const FGlyphCurveSet& Other);
    void ApplyScaleOffset(float Scale, float Offset);
    void ApplySeedJitter(float Jitter);
    FVector2D GetTimeRange() const;
    bool IsEmpty() const;
};
```

### FKeyFrame

Keyframe data:

```cpp
struct FKeyFrame
{
    FGuid KeyId;
    float Time;
    float FloatValue;
    FVector2D Vector2DValue;
    FLinearColor ColorValue;
    EKeyframeInterpolationType InterpType;
};
```

### FGlyphScheduleBuilder

**File**: `Public/Animation/GlyphScheduleBuilder.h`

```cpp
struct FGlyphScheduleBuilder
{
    static void SetPerGlyphDelay(TArray<FGlyphScheduleInfo>& Schedule, float Delay);
    static void SkipWhitespace(TArray<FGlyphScheduleInfo>& Schedule);
    static void SetDelayAt(TArray<FGlyphScheduleInfo>& Schedule, int32 Index, float Delay);
    static void SkipAt(TArray<FGlyphScheduleInfo>& Schedule, int32 Index, bool bSkip);
    // ... other batch methods
};
```

### FTagNode

**File**: `Public/RichText/TagNode.h`

```cpp
struct FTagNode
{
    FString TagName;
    TMap<FString, FString> Attributes;
    TArray<TSharedPtr<FTagNode>> Children;
    TWeakPtr<FTagNode> Parent;
    int32 RangeBegin;
    int32 RangeEnd;
    bool bIsSelfClosing;
};
```

### FRenderNode

**File**: `Public/RichText/RenderNode.h`

```cpp
struct FRenderNode
{
    ERenderNodeType NodeType;
    FString TagName;
    TMap<FString, FString> Attributes;
    TArray<TSharedPtr<FRenderNode>> Children;
    int32 RangeBegin;
    int32 RangeEnd;
    int32 EntryIndex;     // DataAsset entry index
    FString Text;         // Leaf node text

    static TSharedPtr<FRenderNode> Create();
};
```

### FGlyphCurveOverride

**File**: `Public/Animation/GlyphAnimationLayer.h` (associated struct)

```cpp
struct FGlyphCurveOverride
{
    float OpacityMultiplier = 1.0f;
    float LetterSpacingValue = 0.0f;
    FVector2D ScaleMultiplier = FVector2D(1.0f, 1.0f);
    FVector2D TranslationOffset = FVector2D::ZeroVector;
    float RotationMultiplier = 1.0f;
    FVector2D ShearValue = FVector2D::ZeroVector;
    FVector2D PivotValue = FVector2D(0.5f, 0.5f);
    FVector2D ShadowOffsetValue = FVector2D::ZeroVector;
    FLinearColor ColorMultiplier = FLinearColor::White;
    FLinearColor ShadowColorMultiplier = FLinearColor::Transparent;
};
```

## Enum Definitions

### EAnimationStage

```cpp
UENUM(BlueprintType)
enum class EAnimationStage : uint8
{
    Intro,
    Default,
    Outro,
    None,
    GlyphEditor
};
```

### ETextAnimationBlendMode

```cpp
UENUM(BlueprintType)
enum class ETextAnimationBlendMode : uint8
{
    Additive,
    Override,
    Multiply,
    CrossFade
};
```

### EStagePlayDirection

```cpp
UENUM(BlueprintType)
enum class EStagePlayDirection : uint8
{
    Forward,
    Reverse,
    PingPong
};
```

### ETextAnimationTrackType

```cpp
UENUM()
enum class ETextAnimationTrackType : uint8
{
    Opacity,
    LetterSpacing,
    Rotation,
    Translation,
    Scale,
    Shear,
    Pivot,
    ShadowOffset,
    Color,
    ShadowColor
};
```

## Next Steps

- **[Runtime API →](runtime-api)** — C++ Runtime module API
- **[Editor API →](editor-api)** — Editor module API
